// student.h  — umbrella header for the test application
#pragma once

#include <vector>
#include "Book.h"
#include "Sort.h"

// (Opcional) forward declaration da função utilitária usada no test
void printArray(const std::vector<Book>& arr, const char* label);
